import { NextResponse } from 'next/server';
export const dynamic='force-dynamic';
export async function GET(){const key=process.env.GNEWS_API_KEY;if(!key)return NextResponse.json({configured:false,articles:[]});try{const q=encodeURIComponent('(football OR soccer OR Premier League OR Champions League OR transfer OR injury OR lineup)');const u=`https://gnews.io/api/v4/search?q=${q}&lang=en&max=10&sortby=publishedAt&apikey=${key}`;const r=await fetch(u,{cache:'no-store'});if(!r.ok)throw new Error();const d=await r.json();return NextResponse.json({configured:true,articles:(d.articles||[]).map(a=>({title:a.title,description:a.description,url:a.url,image:a.image,publishedAt:a.publishedAt,source:a.source?.name}))},{headers:{'Cache-Control':'no-store, max-age=0'}})}catch{return NextResponse.json({configured:true,articles:[],error:'News temporarily unavailable'},{status:502})}}
